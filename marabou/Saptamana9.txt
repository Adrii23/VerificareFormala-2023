Install instructions for Marabou:

In terminal use git clone https://github.com/NeuralNetworkVerification/Marabou
cd into the folder that contains the cloned repo.
Run the following commands
mkdir build
cd build 
cmake .. -DBUILD_PYTHON=ON
cmake --build .

